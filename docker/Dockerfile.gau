# Dockerfile for gau tool
FROM golang:1.21-alpine as builder

RUN apk add --no-cache git
RUN go install github.com/lc/gau/v2/cmd/gau@latest

# Minimal runtime image
FROM alpine:3.18
COPY --from=builder /go/bin/gau /usr/local/bin/gau
